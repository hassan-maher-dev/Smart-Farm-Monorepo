
server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # ده السطر السحري عشان الـ Flutter Web Routing يشتغل بدون 404
    location / {
        try_files $uri $uri/ /index.html;
    }
}